{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "node node_modules/@symfony/webpack-encore/bin/encore.js production --progress && php bin/console cache:clear --env=prod --no-debug && php bin/console cache:warmup --env=prod --no-debug"
  },
  "deploy": {
    "startCommand": "php bin/console doctrine:migrations:migrate --no-interaction --env=prod && /start-container.sh"
  }
}
