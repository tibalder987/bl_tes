<div class="row">
    {% for article in articles %}
        {% include 'article/article.html.twig' with {'article': article} %}
    {% endfor %}
    {{ knp_pagination_render(articles, 'knp_paginator/sliding.html.twig', {}, {'category': category, 'search': search}) }}
</div>
