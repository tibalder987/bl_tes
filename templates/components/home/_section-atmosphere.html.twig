<section class="section-2" aria-labelledby="section-atmosphere-heading">
	<h2 id="section-atmosphere-heading" class="section-2__titles">
		<span class="section-2__title">{{ 's2.title'|trans({}, 'home') }}</span>
		<span class="section-2__subtitle">{{ 's2.subtitle'|trans({}, 'home') }}</span>
		<span class="section-2__subtitle-2">
			{{ 's2.subtitle2'|trans({}, 'home') }}
			<span>{{ 's2.pulse'|trans({}, 'home') }}</span>
		</span>
	</h2>
</section>
